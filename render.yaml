services:
  - type: web
    name: red-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 web/server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
